# План внедрения мобильной версии

Статус: Draft (рабочий документ)
Ответственный: Product/Frontend
Область: адаптация UI ChronoCompass под мобильные устройства

## Цели
- Сделать приложение удобным на экранах телефонов без горизонтального скролла.
- Сохранить ключевые интеракции с колесами на тач-устройствах.
- Не ломать desktop UX.
- Внедрять поэтапно с понятными критериями готовности.

## Что не входит в задачу
- Без изменений математики/движка колес.
- Без редизайна фич за пределами адаптивности и эргономики взаимодействия.
- Без полной смены визуальной темы.

## Брейкпоинты
- Phone: `<= 640px`
- Tablet: `641px - 1024px`
- Desktop: `> 1024px`

## Текущие проблемные точки (по ревью кода)
- Главный контейнер форсит desktop-ширину в `src/App.svelte` (`clamp(1200px, ...)`).
- Сетка доски стартует с 2 колонок в `src/components/Board.svelte`.
- В `src/components/Header.svelte` нет явного mobile-layout.
- Панели колес (`Compass/Cycle`) используют desktop-плотность и размеры.
- Тултипы заточены под desktop-поповеры, что неудобно на тач-экранах.

## Техническое направление
- Mobile-first стратегия в CSS.
- Сохранение текущей структуры компонентов.
- Добавление compact-режимов по брейкпоинтам.
- Для телефонов: sheet-представление деталей там, где floating tooltip неустойчив.
- Для desktop: переход к модели **Resizable Dashboard Grid (masonry-like)** с сохранением размеров карточек.

## Обязательные продуктовые требования
1. Все тултипы в мобильной версии раскрываются на весь экран.
2. Один экран = одна карточка колеса.
3. Все колеса доски размещаются в циклической карусели.
4. Переключение колес: по горизонтальным кнопкам и горизонтальным свайпом.
5. На desktop карточки колес изменяются пользователем по ширине и высоте (resize).
6. Размеры каждой карточки сохраняются в данных колеса.
7. В ряду может быть произвольное количество колес, высота ряда не фиксирована.
8. Для desktop задается дефолтный размер карточки:
   - в этом размере создаются новые карточки на доске;
   - у каждой карточки есть кнопка «сброс размера к дефолту».
9. Карточка колеса логически разделяется на 2 части:
   - квадратный блок с визуальным колесом;
   - блок текстовой информации (инфоблок).
10. В хедере карточки размещаются 2 отдельные кнопки collapse/expand:
   - кнопка для визуального квадрата;
   - кнопка для инфоблока;
   - обе кнопки расположены рядом, между кнопками перемещения карточки и кнопкой `i`.
11. Инфоблок выносится в отдельный компонент `InfoControl` с функционалом:
   - настройка набора видимых строк из доступных для данного колеса;
   - настройка очередности строк;
   - настройка текстовых пометок (лейблов) строк.
12. Высота инфоблока изменяется независимо от общей карточки, чтобы обеспечить место под все выбранные строки.
13. Поддерживаются 4 состояния карточки:
   - полностью свернута (виден только хедер);
   - развернут только визуальный квадрат;
   - развернут только инфоблок;
   - развернуты обе части.
14. При уменьшении/увеличении карточки все элементы масштабируются пропорционально:
   - размеры и шрифты хедера;
   - элементы визуального квадрата;
   - элементы инфоблока.

## Поэтапный план

### Этап 1: Каркас приложения и сетка доски
- Обновить контейнер в `src/App.svelte` на fluid-layout для малых экранов.
- Для мобильной версии заменить сетку доски на режим «1 карточка на экран».
- Реализовать карусель колес с циклической навигацией по кругу.
- Добавить два способа переключения: кнопки влево/вправо и горизонтальный свайп.
- Для desktop/tablet внедрить `Resizable Dashboard Grid (masonry-like)`:
  - ручной resize карточек по ширине/высоте;
  - дефолтный размер карточки для создания новых колес;
  - reset карточки к дефолтному размеру;
  - сохранение размеров в данных колеса;
  - нефиксированная высота рядов и произвольная плотность карточек.
- Для контента карточки:
  - разделить карточку на квадрат колеса и отдельный инфоблок;
  - добавить 2 кнопки collapse/expand в хедере (для квадрата и для инфоблока);
  - разместить эти кнопки между move-кнопками и кнопкой `i`;
  - реализовать 4 состояния карточки (header-only / visual-only / info-only / full);
  - внедрить `InfoControl` как отдельный компонент управления строками инфоблока;
  - сделать независимый resize высоты инфоблока;
  - внедрить пропорциональное масштабирование размеров и шрифтов всей карточки.
- Убрать/исправить все источники горизонтального переполнения.
- Проверить sticky-header с учетом safe-area.

Критерии приёмки:
- Нет горизонтального скролла на ширинах 360-430px.
- На телефоне одновременно видна только одна карточка колеса.
- Карусель циклическая: после последнего колеса переход к первому и наоборот.
- Переключение работает и по кнопкам, и по свайпу.
- На desktop изменение размеров карточки сразу отражается в layout.
- После перезагрузки размеры карточек восстанавливаются из сохраненных данных.
- Новая карточка создается в дефолтном размере.
- Кнопка reset корректно возвращает размер карточки к дефолтному.
- В хедере работают 2 независимые кнопки collapse/expand (visual/info).
- Карточка корректно поддерживает все 4 состояния (header-only / visual-only / info-only / full).
- `InfoControl` позволяет менять видимость, порядок и пометки строк.
- Высота инфоблока регулируется отдельно от общей карточки.
- При ресайзе карточки шрифты и размеры внутренних элементов меняются пропорционально.

### Этап 2: Header и глобальные контролы
- Внедрить mobile-layout для `Header.svelte` (компактные logo/title, перенос контролов).
- Обеспечить доступность `TimePicker` и `LocationPicker` без наложений.
- Проверить тач-эргономику кнопок действий.

Критерии приёмки:
- Header стабилен при скролле.
- Контролы не обрезаются и не перекрываются на phone-width.

### Этап 3: Адаптивность карточек колес (Compass/Cycle)
- Добавить compact-режим типографики и отступов в info-блоках.
- Подогнать кластеры кнопок (nav/toggles) под удобные тач-зоны.
- Проверить usability SVG-интеракций на coarse pointers.

Критерии приёмки:
- Основные действия с колесом доступны одной рукой.
- Нет конфликтов между контролами и областью графика.

### Этап 4: Mobile UX для тултипов и модалок
- Добавить phone-поведение для `CompassTooltip` и `CycleTooltip`.
- В мобильной версии тултипы открываются в fullscreen-режиме (на весь экран).
- На desktop оставить floating tooltip-поведение.
- Проверить mobile-layout модалок (`WheelControl`, `WheelPicker`, `LocationPicker`, `MomentControl`) и поведение с виртуальной клавиатурой.

Критерии приёмки:
- Панели деталей читаемы и скроллятся на телефонах.
- Любой tooltip на телефоне занимает весь экран и корректно закрывается.
- Фокус в input не ломает верстку (включая keyboard resize).

### Этап 5: Производительность и полировка
- Проверить стоимость анимаций и blur/shadow на мобильных GPU.
- При необходимости упростить тяжелые эффекты в compact-режиме.
- Финальный проход по accessibility (tap targets, focus, contrast, semantics).

Критерии приёмки:
- Плавная работа на современных мобильных браузерах.
- Базовые требования доступности соблюдены для touch + keyboard.

## Технический чеклист
- [ ] Обновить responsive-контейнер в `src/App.svelte`
- [ ] Реализовать phone-карусель «1 карточка на экран» в `src/components/Board.svelte`
- [ ] Добавить циклическое переключение колес по кнопкам
- [ ] Добавить горизонтальный свайп для переключения колес
- [ ] Спроектировать desktop-layout как `Resizable Dashboard Grid (masonry-like)`
- [ ] Добавить resize карточек по ширине/высоте на desktop
- [ ] Ввести дефолтный размер карточки для новых колес
- [ ] Добавить кнопку reset размера карточки к дефолту
- [ ] Сохранить размеры карточек в данных колеса и восстановление при загрузке
- [ ] Разделить карточку на квадрат колеса и отдельный инфоблок
- [ ] Добавить 2 кнопки collapse/expand в хедере (visual/info)
- [ ] Разместить эти кнопки между move-кнопками и кнопкой `i`
- [ ] Реализовать 4 состояния карточки (header-only / visual-only / info-only / full)
- [ ] Вынести инфоблок в отдельный компонент `InfoControl`
- [ ] Реализовать в `InfoControl` настройку видимости строк
- [ ] Реализовать в `InfoControl` настройку порядка строк
- [ ] Реализовать в `InfoControl` настройку текстовых пометок строк
- [ ] Реализовать независимый resize высоты инфоблока
- [ ] Реализовать пропорциональное масштабирование размеров и шрифтов содержимого карточки
- [ ] Добавить mobile-layout в `src/components/Header.svelte`
- [ ] Добавить compact-режимы в `src/components/Compass.svelte`
- [ ] Добавить compact-режимы в `src/components/Cycle.svelte`
- [ ] Добавить fullscreen mobile-режим в `src/components/CompassTooltip.svelte`
- [ ] Добавить fullscreen mobile-режим в `src/components/CycleTooltip.svelte`
- [ ] Проверить mobile-fit модалки `src/components/WheelPicker.svelte`
- [ ] Проверить mobile-fit модалки `src/components/WheelControl.svelte`
- [ ] Проверить keyboard-safe layout в `src/components/LocationPicker.svelte`
- [ ] Кросс-девайс QA (iOS Safari / Android Chrome)

## Матрица тестирования
- Устройства:
  - iPhone (Safari)
  - Android (Chrome)
  - Desktop (Chrome/Firefox/Safari)
- Ширины:
  - 360, 390, 430, 768, 1024, 1280+
- Сценарии:
  - 1 колесо, 4+ колес на доске
  - Открытие тултипа с длинным контентом
  - Многократное открытие/закрытие модалок
  - Редактирование времени/локации, добавление/редактирование колес

## Риски
- Конфликты layering между sticky-header, tooltip и modal.
- Различия поведения viewport resize при клавиатуре (iOS vs Android).
- Просадка производительности из-за тяжелых эффектов на старых телефонах.

## Открытые вопросы
- Нужно ли снижать интенсивность анимаций глобально на coarse pointers?
- Нужен ли ручной переключатель compact mode или достаточно авто по брейкпоинтам?

## Журнал изменений
- 2026-03-01: Создана начальная русская версия плана.
- 2026-03-02: Добавлены требования по desktop default size/reset, двухчастной структуре карточки и компоненту `InfoControl`.
- 2026-03-02: Уточнены 2 кнопки collapse в хедере, 4 состояния карточки и пропорциональное масштабирование UI при ресайзе.
